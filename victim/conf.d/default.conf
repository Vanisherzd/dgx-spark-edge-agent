server {
    listen 80;
    server_name _;

    location / {
        add_header Content-Type text/plain;
        return 200 "ok";
    }

    location /api/ {
        proxy_pass http://edge-victim-app:80/;
        proxy_connect_timeout 2s;
        proxy_read_timeout 5s;
    }
}